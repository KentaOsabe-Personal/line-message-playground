interface:
  display_name: "Validate Implementation"
  short_description: "Validate integration across approved parent-task units"
  default_prompt: "Use $kiro-validate-impl to validate feature integration across approved parent-task units."

policy:
  allow_implicit_invocation: false
