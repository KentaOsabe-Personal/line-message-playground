# LINEリッチメニュー機能 要件定義インプット

## 文書の位置付け

この文書は、LINE Message PlaygroundへLINEリッチメニュー機能を追加するために、対話で確定したプロダクト判断を要件定義の入力として整理したものである。正式なKiro Requirements、Design、Tasksの承認を代替するものではない。

ドメイン用語は [CONTEXT.md](./CONTEXT.md)、判断理由は [docs/adr/](./docs/adr/) を正とする。

## 背景と目的

現在のアプリは、ownerが複数の登録済みLINE Messaging APIチャネルを管理し、自分を中心とした安全な配信・Webhook検証を行う環境である。現状はLINEリッチメニューを作成・設定する機能を持たない。

初期版では、ownerがアプリ組み込みのテンプレートを選び、各項目の表示名とHTTPSリンク先を設定し、対象チャネルの全利用者向け既定リッチメニューとして安全に適用できることを目指す。将来、社用端末のLINEアカウントを友だち登録した場合にも、同じチャネル既定リッチメニューを表示対象とする。

## 対象利用者と権限

- 利用者は、既存のowner認証を通過した本人だけとする。
- ownerのLINE identityと同一providerに属する登録済みチャネルだけを対象とする。
- 有効なチャネルでは、リッチメニューの作成・プレビュー・適用・解除・状態確認・履歴確認を行える。
- 無効なチャネルでは、リッチメニュー実状態と適用履歴の閲覧だけを許可し、新しい変更操作は許可しない。

## 初期版の機能範囲

### 対象に含めるもの

- チャネル単位のリッチメニュー管理画面
- 組み込みテンプレートカタログ
- テンプレート選択と全項目の入力
- 日本語表示名を含むリッチメニュー画像生成
- スマートフォン相当のプレビュー
- チャネル既定リッチメニューの即時適用
- 適用済みメニューの編集・置換
- チャネル既定リッチメニューの適用解除
- LINE上の実状態確認とアプリ保存状態との照合
- アプリ外リッチメニュー検出時の警告
- 外部変更検出後の再適用または管理終了
- 適用・置換・解除・管理終了の履歴
- 結果不明状態からの再確認
- 旧メニュー削除失敗時の明示的な後片付け
- チャネル無効化・再有効化・物理削除との整合

### 初期版の対象外

- 利用者別リッチメニューの作成・検出・解除・管理
- postback、メッセージ、日時選択、リッチメニュー切替などURI以外のアクション
- ownerによる画像アップロード
- ownerによる自由な座標・レイアウト編集
- ownerによる色、背景、フォントの変更
- ownerによるテンプレート追加
- カスタムテンプレート
- リッチメニューの表示数・クリック数などの利用統計
- 日時予約、公開開始日時、公開終了日時
- LINE Official Account Manager上のリッチメニュー編集・削除
- 過去メニューへのロールバック
- 生成画像ファイルの履歴保存

## 表示対象と優先順位

- 初期版は、対象チャネルのトーク画面を開くすべての利用者へ表示されるチャネル既定リッチメニューを扱う。
- 利用者別リッチメニューが設定されている利用者には、利用者別リッチメニューが優先される。
- 初期版は利用者別設定を変更せず、管理画面と適用結果に優先順位の注意を表示する。
- LINE Official Account Manager側にも既定リッチメニューがある場合、Messaging APIで設定したチャネル既定リッチメニューが優先される。

## テンプレートカタログ

### 初期テンプレート

| テンプレート | 構成 | 想定画像サイズ | 必須項目数 |
| --- | --- | --- | --- |
| コンパクト・2項目 | 1行2列 | 2500 × 843 | 2 |
| コンパクト・3項目 | 1行3列 | 2500 × 843 | 3 |
| ラージ・6項目 | 2行3列 | 2500 × 1686 | 6 |

### テンプレート共通契約

- 初期版のカタログはアプリ組み込みテンプレートだけで構成する。
- 将来、別種のテンプレート供給元を追加できるよう、テンプレートには安定したIDと版を持たせる。
- 3種類のテンプレートは同じデザイン体系を共有し、項目数と配置だけを変える。
- デザインは現在のWebアプリに合わせ、白、淡いグリーン、濃い文字色を基調とする。
- 背景、配色、フォント、文字領域、タップ領域、アクション種別はテンプレートが所有する。
- 特定用途を誤認させる固定の意味付きアイコンは使用しない。
- ownerは選択したテンプレートの全項目を入力しなければならない。
- 未使用項目、項目の自動詰め替え、部分入力は許可しない。

### テンプレート版

- テンプレートのデザインまたは配置規則を変更する場合は、テンプレート版を更新する。
- 新しいテンプレート版をリリースしても、LINEへ適用済みのメニューは自動変更しない。
- 管理画面は新しい版の存在を案内できるようにする。
- ownerが新しい版のプレビューを確認して再適用した場合だけ、LINE上のメニューを置換する。

## リッチメニュー項目

### 入力内容

各リッチメニュー項目は、次の組で構成する。

- 表示名
- HTTPSリンク先

### 表示名

- 表示名は必須とする。
- ownerによる改行入力は許可しない。
- アプリへ同梱する日本語フォントで描画できる文字だけを許可する。
- 日本語、英数字、一般的な記号を対象とし、絵文字や未対応文字は拒否する。
- テンプレートの所定領域へ最大2行で自動配置する。
- 最大2行でも収まらない表示名は拒否し、プレビューを作成しない。
- 収めるための無制限なフォント縮小は行わない。

### リンク先

- リンク先は必須とする。
- URI schemeは`https`だけを許可する。
- hostnameが存在し、ポートを含むURL構文が正しいことを検証する。
- URL構文上のusernameまたはpasswordを含むURLは拒否する。
- クエリ文字列とフラグメントは許可する。
- クエリ文字列内の秘密値をアプリが推測・マスキングすることはしない。
- 入力した完全なURLが現在設定と適用履歴へ保存・表示されることをownerへ案内する。
- アクセストークンなどの秘密値をリンク先へ含めないようownerへ案内する。
- Backendはリンク先へ自動接続せず、到達可能性を保証しない。
- プレビューからownerが明示的にブラウザで開く「リンクをテスト」導線を提供する。

## 画像生成と表示契約

- ownerの表示名入力から、テンプレートの背景と文字配置を合成した最終画像を生成する。
- 画像生成は同じテンプレート版と同じ入力から同じ結果を得られる決定的な処理とする。
- LINEのリッチメニュー画像要件を満たすPNGまたはJPEGを生成する。
- 管理対象リッチメニューは、利用者がトーク画面を開いたときに展開された状態を初期表示とする。
- 開閉用チャットバーの文言は、全テンプレートで「メニュー」に固定する。
- LINE API上で既定設定を確認できた状態は「LINE適用済み」とし、端末表示成功とは区別する。
- 適用後はスマートフォンでトーク画面を開き直す確認手順を案内する。
- 端末上の表示確認結果は初期版では保存しない。

## 管理画面と編集体験

- 既存の各チャネルカードに「リッチメニュー管理」への導線を追加する。
- 選択したチャネル専用の管理画面を開き、対象チャネル名と公開IDを明示する。
- 適用済みメニューの編集開始時は、現在のテンプレート、表示名、リンク先を初期表示する。
- テンプレートを変更すると各位置の意味が変わるため、全項目をクリアする。
- 入力済み項目がある場合は、テンプレート変更前に入力消去を確認する。
- ownerが消去を確定した場合だけテンプレートを変更し、キャンセル時は入力を維持する。
- 未適用の変更がある状態で画面を閉じる、または別画面へ移動する場合は、入力破棄を確認する。
- ownerセッションが失効した場合は、未適用の表示名、リンク先、プレビューを消去する。
- 入力値をブラウザの永続ストレージへ保存しない。

## プレビュー

- 適用前にリッチメニュープレビューを必須とする。
- プレビューには、対象チャネル、テンプレートID・版、生成画像、全表示名、全リンク先を表示する。
- ownerはプレビューから各リンク先を手動テストできる。
- プレビュー確認前にLINE上の状態を変更しない。
- 下書きはBackendへ保存しない。
- プレビューの有効期限は、既存配信プレビューと同じ10分とする。
- 有効期限を画面へ表示する。
- 期限切れ後は入力内容を画面に残し、再プレビューを求める。
- 対象チャネル、現在の既定メニュー、テンプレート版のいずれかが変わった場合はプレビューを失効させる。
- 失効プレビューからの適用を拒否し、最新状態からの再プレビューを求める。
- プレビュー確認にはowner、対象チャネル、チャネルrevision、現在の既定メニュー、テンプレートID・版、全項目、生成画像の同一性を結び付ける。

## 適用操作

### 基本フロー

1. 最新のチャネル状態とLINE上のリッチメニュー実状態を取得する。
2. ownerがテンプレートと全項目を入力する。
3. Backendが入力を検証し、最終画像とプレビューを生成する。
4. ownerが対象チャネル、画像、全表示名、全リンク先、外部設定警告を確認する。
5. ownerが適用を確定する。
6. Backendがプレビューの期限と全revisionを再検証する。
7. LINE上に新しいリッチメニューを作成する。
8. 生成画像をアップロードする。
9. 新しいリッチメニューをチャネル既定へ設定する。
10. LINE上の既定IDを再取得し、適用結果を確認する。
11. 以前の管理対象リッチメニューがあれば削除する。
12. 適用結果と履歴を表示する。

### 冪等性と競合

- 一つの確認済みプレビューから、一意な適用操作を作成する。
- 同じ適用操作が繰り返し要求されても、複数のリッチメニューを作らず同じ結果へ収束させる。
- Frontendのボタン無効化だけを二重適用防止の根拠にしない。
- Backendが受理した適用操作は、ownerセッションやブラウザ通信が失効しても操作IDで追跡を続ける。
- 再認証後は、新しい適用を開始せず、既存操作の結果を取得できるようにする。
- 適用中は、対象チャネルの別のリッチメニュー変更とチャネル状態変更を競合として拒否する。

### 結果分類

- `LINE適用済み`: LINE API上で管理対象リッチメニューがチャネル既定であることを確認できた。
- `failed`: LINE上の既定状態を変更していないと確定でき、処理を失敗として完了できた。
- `適用要確認`: タイムアウトなどにより、LINE上の適用結果を成功または失敗と断定できない。
- `後片付け必要`: 新メニューの適用成功は確定したが、旧管理対象メニューの削除だけが完了していない。

### 結果不明

- 結果不明を成功または失敗と推測しない。
- 自動的に新しい適用を開始しない。
- LINE上の実状態を再確認して既存操作へ収束するまで、新しい適用・解除・チャネル無効化を許可しない。

### 後片付け

- 新メニューの適用成功後に旧メニュー削除だけが失敗した場合、適用自体は成功として表示する。
- 「旧メニューの後片付けが必要」と明示する。
- 現在表示中の新メニューを変更せず、旧メニュー削除だけを再試行する。
- 後片付けが完了するまで、新しい適用・解除・チャネル無効化を許可しない。
- 再試行はownerが「後片付けを再試行」を明示的に実行した場合だけ行う。
- 画面表示、時間経過、定期処理を契機とする自動再試行は行わない。

## LINE上の実状態とアプリ外変更

### 確認タイミング

- 管理画面を開いたときにLINE上の実状態を確認する。
- 適用、解除、管理終了、チャネル無効化の直前にも再確認する。
- 確認中、確認成功、確認失敗、結果不明を画面上で区別する。

### アプリ外リッチメニュー

- LINE Official Account Manager、別アプリ、手作業のMessaging API呼び出しで作成・設定されたメニューをアプリ外リッチメニューとする。
- アプリ外リッチメニューの存在を検出した場合は、適用確認で警告する。
- 新しい管理対象リッチメニューを既定へ設定すると、既存のアプリ外メニューが既定ではなくなる、または優先表示されなくなる可能性を説明する。
- ownerの確認後は管理対象リッチメニューをチャネル既定として設定できる。
- アプリ外リッチメニューの内容を編集しない。
- アプリ外リッチメニュー資源を削除しない。

### 外部変更

- アプリが追跡する管理対象リッチメニューとLINE上の実状態が一致しない場合は、外部変更として表示する。
- 以前の管理対象メニューを自動復元しない。
- LINE上の現在状態を管理対象として自動取り込みしない。
- ownerは、以前の設定を再プレビューして再適用するか、このアプリでの管理を終了するか選択できる。

### 管理終了

- 管理終了時は、このアプリが以前作成したリッチメニューがLINE上に残っていれば、それだけを削除する。
- 現在表示中のアプリ外リッチメニューには触れない。
- 管理終了の事実を適用履歴へ記録する。

## 適用解除

- ownerは管理対象リッチメニューの「適用を解除」を実行できる。
- 適用解除前に、対象チャネル、現在のメニュー、解除後の影響を確認する。
- Messaging API側のチャネル既定指定を解除する。
- 解除結果をLINE上の実状態から確認する。
- 解除確認後に管理対象リッチメニュー資源をLINEから削除する。
- Official Account Manager側の既定メニューがある場合、それが再び表示される可能性を警告する。
- 適用解除を履歴へ記録する。

## チャネルライフサイクルとの関係

### チャネル無効化

- 管理対象リッチメニューが適用中のチャネルを無効化する場合、同じowner操作の中で適用解除を行う。
- LINE上の適用解除を確認できた場合だけチャネル無効化を完了する。
- 解除結果を確定できない場合は、チャネルを無効と断定せず「無効化要確認」とする。
- 無効化要確認からLINE上の状態を再確認して収束するまで、対象チャネルの関連操作を許可しない。
- LINE上の解除結果にかかわらずDBだけを無効化する処理は行わない。

### チャネル再有効化

- チャネルを再有効化しても、以前の管理対象リッチメニューを自動復元しない。
- リッチメニューが必要な場合は、ownerが改めてテンプレートと内容を確認し、通常の適用フローから設定する。

### チャネル物理削除

- 管理対象リッチメニューが適用中である間は、チャネル物理削除を許可しない。
- 現在適用中の管理対象リッチメニューがなく、配信・配信先・Webhookなど既存の別参照もない場合は、リッチメニュー適用履歴があってもチャネル物理削除を許可する。
- チャネル物理削除時は、そのチャネルのリッチメニュー適用履歴も削除する。
- 削除済みチャネル向けのリッチメニュースナップショットは保持しない。

## 適用履歴

### 記録対象

- 適用
- 置換
- 適用解除
- 管理終了
- 失敗
- 適用要確認からの収束
- 後片付け結果

### 保存内容

- 操作ID
- 操作種別
- テンプレートIDと版
- 各項目の表示名と完全なHTTPSリンク先
- LINEのリッチメニューID
- 操作開始日時と完了日時
- 安全な結果分類

### 保持と削除

- LINEから旧メニュー資源を削除した後も、対象チャネルが存在する間は履歴を保持する。
- 履歴はロールバック資源として扱わない。
- 生成画像ファイルは履歴へ保存しない。
- 対象チャネルを物理削除した場合は、リッチメニュー適用履歴も削除する。
- アクセストークン、チャネルシークレット、LINEの生応答、秘密を含むログを履歴へ保存しない。

## エラーと回復

- 入力エラー、認証失効、チャネル無効、資格情報利用不能、認証失敗、利用制限、LINE利用不能、結果不明、外部変更、後片付け必要を区別する。
- LINE SDKまたはHTTPクライアントの生例外をFrontendへ返さない。
- LINEの生レスポンス、認証情報、生成画像のバイナリを通常ログへ出さない。
- 429または利用制限時に自動再試行しない。
- タイムアウト時に自動で新しいリッチメニューを作成しない。
- 結果不明時は状態確認を優先し、別操作としての再適用を許可しない。
- 既定設定前に作成した候補メニューが不要と確定した場合は、このアプリが作成した候補だけを削除する。
- 候補削除に失敗した場合は追跡可能な後片付け対象とし、黙って放置しない。

## セキュリティとプライバシー

- 既存のowner session、同一provider確認、exact-origin CSRFを再利用する。
- チャネルアクセストークンはBackendだけが復号・使用する。
- FrontendからLINE Messaging APIを直接呼び出さない。
- リンク先、表示名、テンプレートID、操作ID以外の秘密値をFrontendへ返さない。
- 確認トークンへ表示名や完全URLを直接含めず、同一性を検証できるdigestとrevisionを用いる。
- 余剰フィールド、不正型、非正規URL、未対応文字を境界で拒否する。
- owner入力をHTMLとして解釈せず、画像描画用テキストとして扱う。
- Backendは入力URLへ接続しない。
- リッチメニュー画像と履歴へLINE user IDを含めない。

## 非機能要件の入力

- 同じテンプレート版と入力から同じ画像を生成できること。
- 生成画像がLINEの寸法、形式、アスペクト比、ファイルサイズ制約を満たすこと。
- 画像内の文字が領域外へはみ出さず、最大2行で読み取れること。
- 3種類すべてのタップ領域と画像領域が一致すること。
- 同じ適用操作の並行実行が一つのLINE外部作用へ収束すること。
- LINE外部通信中に長時間のDB lockを保持しないこと。
- 外部通信後はチャネルrevisionと現在状態を再確認してから結果を採用すること。
- チャネル単位で競合操作を制御し、別チャネルの操作を不必要に止めないこと。
- アプリ外リッチメニューを削除しないことを、統合テストで証明すること。
- タイムアウト、429、認証失敗、アップロード失敗、既定設定失敗、旧メニュー削除失敗を個別に再現できること。
- FrontendとBackendのテスト定義には、プロジェクト標準の日本語による「テストケース:」「期待値:」コメントを付けること。

## 将来拡張のために残す余地

- 組み込み以外のテンプレート供給元
- owner作成テンプレート
- 画像アップロード
- アイコン選択
- 利用者別リッチメニュー
- postbackなどURI以外のアクション
- リッチメニュー切替とalias
- 利用統計
- 日時予約と公開期間

将来拡張の余地は、初期版の公開APIや永続モデルへ未使用の汎用機能を先行実装することを意味しない。初期版は、安定したテンプレートID・版、アクション種別を閉じ込める境界、チャネル単位の管理契約を持つことで拡張可能性を確保する。

## 要件化前にDesignで確定する技術事項

- 新しいBackend appの責任境界と既存`linechannels` appとの連携方式
- テンプレートカタログのコード上の表現と版管理方式
- 同梱する日本語フォントとライセンス
- 画像生成ライブラリ、文字幅計測、折返し規則、ファイルサイズ保証
- プレビュー確認トークンのpayloadとdigest構成
- リッチメニュー操作状態機械とDB schema
- LINE gatewayのendpoint構成、timeout、エラー分類
- 結果不明からの照合アルゴリズム
- チャネル無効化をまたぐ状態遷移とreference contract
- 履歴一覧のページングと表示件数
- API DTO、HTTP status、Frontend state machine
- テンプレート画像の最終的な余白、罫線、文字サイズ

## Spec分割検討の入力

この機能は、テンプレート・画像生成、LINE上のリッチメニュー資源ライフサイクル、owner向け管理UI、外部変更との照合、チャネル無効化・削除との統合という複数の責任境界を持つ。正式なDiscoveryでは、`.kiro/steering/spec-sizing.md`に従って実行可能タスク数を見積もり、単一Specで40件以上が見込まれる場合は、次の候補を依存順に分割する。

1. リッチメニューテンプレート・画像生成・LINE資源管理の基盤
2. owner向け管理UI・状態照合・チャネルライフサイクル統合

分割する場合も、利用者成果が途中で矛盾しないよう、上流の管理契約と下流UIの統合点を各Specのbriefで明示する。

